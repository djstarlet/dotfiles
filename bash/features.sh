# dotfiles feature flags - edit and open a new terminal
#
# Deployed to ~/.config/dotfiles/features.sh by install.sh and sourced from
# ~/.bashrc. install.sh only creates this file when missing, so your edits
# survive re-runs. 1 = on, 0 = off.

dotfiles_blesh=1   # as-you-type completion & syntax highlighting (ble.sh; needs the blesh/ble.sh package)
